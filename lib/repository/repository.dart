
import '../Utils/database_controller.dart';

class Repository {
  final DatabaseController dbController = DatabaseController();

  // Future<List<CryptoCurrencies>> getAllMarkers() => dbController.getAllMarkers();

  // Future<int> insertMarkers(CryptoCurrencies? todo) => dbController.createMarkers(todo);

  // Future updateTodo(CryptoCurrencies? todo) => dbController.updateTODO(todo);

  // Future deleteMarkers(int id) => dbController.deleteTodo(id);
}
